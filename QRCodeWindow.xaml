<Window x:Class="COMIGHT.QRCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:COMIGHT"
        mc:Ignorable="d"
        Title="QR Code Generator" Height="480" Width="480" FontSize="13" WindowStartupLocation="CenterScreen">
    
    <Grid Margin = "3,3,3,3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="Input Text:" Margin="5" />

        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="5" HorizontalAlignment="Stretch" TextWrapping="Wrap" TextChanged="InputTextBox_TextChanged" />

        <Image x:Name="QRImage" Grid.Row="2" Margin="10" Stretch="Uniform"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Save" Click="BtnSave_Click"/>
            <Button Content="Exit" Click="BtnExit_Click"/>
        </StackPanel>
    </Grid>
</Window>
